# Repository Agent Rules

## Versioning

- On every code update, always increment the userscript version in `discord-ai-translator.user.js` (`// @version`).
- Do not skip version bumps, including small fixes or documentation-related script changes.

